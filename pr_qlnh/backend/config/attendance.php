<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Cấu hình giờ làm việc
    |--------------------------------------------------------------------------
    */
    'work_hours' => [
        'start_time' => '08:00:00',          // Giờ bắt đầu làm việc
        'end_time' => '17:30:00',            // Giờ kết thúc làm việc
        'late_threshold' => '08:15:00',      // Ngưỡng đi muộn
        'early_leave_threshold' => 15,       // Phút được phép về sớm (15 phút)
    ],

    /*
    |--------------------------------------------------------------------------
    | Cấu hình giờ nghỉ trưa
    |--------------------------------------------------------------------------
    */
    'lunch_break' => [
        'start_time' => '12:00:00',          // Bắt đầu nghỉ trưa
        'end_time' => '13:00:00',            // Kết thúc nghỉ trưa
        'duration_hours' => 1,               // Thời gian nghỉ (giờ)
    ],

    /*
    |--------------------------------------------------------------------------
    | Cấu hình chấm công
    |--------------------------------------------------------------------------
    */
    'check_in' => [
        'earliest_time' => '06:00:00',       // Giờ sớm nhất được chấm công vào
        'latest_time' => '12:00:00',         // Giờ muộn nhất được chấm công vào
    ],

    /*
    |--------------------------------------------------------------------------
    | Cấu hình giờ làm việc
    |--------------------------------------------------------------------------
    */
    'hours' => [
        'standard_work_hours' => 8,          // Số giờ làm việc tiêu chuẩn
        'minimum_work_hours' => 4,           // Số giờ làm việc tối thiểu
    ],
];
